import chai from 'chai'
chai.should()

describe('Invoice', () => {

  it(`Object attributes`, (done) => {
    done()
  })

  it(`Export to HTML`, (done) => {
    done()
  })

  it(`Export to PDF`, (done) => {
    done()
  })

})
